name: catalogo_productos
description: "Catálogo de productos - Práctica"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.5.3
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Añade aquí la imagen local usada por al menos un producto.
  assets:
    - assets/images/local_product.png
